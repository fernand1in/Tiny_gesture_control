# 🖐️ tiny gesture control
一款轻量级的手势识别控制工具，基于 mediapipe + pyqt6 开发，无需手动键鼠操作，通过手势即可操控电脑 🚀

## 📋 功能特性
- ✨ 多手势识别：支持点赞、拇指向下、ok、手指交叉、数字1等手势识别
- 🎵 媒体控制：上一首/下一首/播放暂停/音量调节/快进/快退
- 💻 系统操作：窗口切换/最小化/截屏/静音
- 🎨 可视化界面：pyqt6 图形界面，支持自定义手势-操作映射
- ⚙️ 配置持久化：手势映射配置自动保存，重启后无需重新设置
- 📱 跨兼容：适配 windows 系统，支持摄像头实时识别

## 🛠️ 环境要求
| 依赖 | 版本 | 说明 |
|------|------|------|
| python | 3.9/3.10 | 推荐3.10（兼容性最佳） |
| pyqt6 | 6.6.1 | 稳定版，避免dll加载问题 |
| mediapipe | 0.10.9 | 手势识别核心库 |
| opencv-python | 4.8.1.78 | 摄像头采集与图像处理 |
| pyautogui | 0.9.54 | 键鼠模拟操作 |
| numpy | 1.24.3 | 数值计算支持 |

## 🚀 快速开始

### 1. 克隆项目
直接下载项目文件，或通过git克隆到本地，进入项目目录

### 2. 创建虚拟环境（推荐）
创建名为 gesture-env 的虚拟环境，激活环境：
- windows：gesture-env\scripts\activate
- linux/macos：source gesture-env/bin/activate

### 3. 安装依赖
先卸载可能冲突的旧版本依赖，再安装稳定版本：
卸载命令：pip uninstall -y pyqt6 mediapipe opencv-python
安装命令：pip install pyqt6==6.6.1 mediapipe==0.10.9 opencv-python==4.8.1.78 pyautogui==0.9.54 numpy==1.24.3

### 4. 运行程序
在激活的虚拟环境中执行：python main.py

### 5. 打包为可执行文件
文件夹模式（推荐，启动快）：
pyinstaller --windowed --name tiny_gesture_ctl --add-data "gesture_config.json;." --hidden-import mediapipe --hidden-import cv2 main.py
单文件模式（仅一个exe）：
pyinstaller --onefile --windowed --name tiny_gesture_ctl --add-data "gesture_config.json;." --hidden-import mediapipe --hidden-import cv2 main.py
打包后程序位于 dist/ 目录下

## 🎯 手势映射说明
| 手势 | 默认绑定操作 | 可自定义替换 |
|------|--------------|--------------|
| 👍 点赞 | 上一首 | ✅ |
| 👇 拇指向下 | 下一首 | ✅ |
| 👌 ok手势 | 播放/暂停 | ✅ |
| 🤞 手指交叉 | 音量+ | ✅ |
| 1️⃣ 数字1 | 音量- | ✅ |

## ⚙️ 自定义扩展
### 新增功能步骤
1. 修改 key_options 新增下拉框选项，例如添加快进、快退：
key_options = ["无操作", "上一首", "下一首", "播放/暂停", "音量+", "音量-", "静音", "切换窗口", "快进", "快退", "最小化窗口", "截屏"]
2. 修改 key_actions 实现功能逻辑：
key_actions = {
    "无操作": lambda: none,
    "上一首": lambda: pyautogui.press("prevtrack"),
    "下一首": lambda: pyautogui.press("nexttrack"),
    "播放/暂停": lambda: pyautogui.press("playpause"),
    "音量+": lambda: pyautogui.press("volumeup"),
    "音量-": lambda: pyautogui.press("volumedown"),
    "静音": lambda: pyautogui.press("volumemute"),
    "切换窗口": lambda: pyautogui.hotkey("alt", "tab"),
    "快进": lambda: pyautogui.press("right"),
    "快退": lambda: pyautogui.press("left"),
    "最小化窗口": lambda: pyautogui.hotkey("win", "d"),
    "截屏": lambda: pyautogui.hotkey("win", "shift", "s")
}

## ❗ 常见问题
### q1: 导入pyqt6报dll加载失败？
a: 安装vc++运行库，下载地址：https://learn.microsoft.com/zh-cn/cpp/windows/latest-supported-vc-redist?view=msvc-170

### q2: mediapipe报_framework_bindings缺失？
a: 重新安装完整版本：
卸载命令：pip uninstall -y mediapipe
安装命令：pip install mediapipe==0.10.9 --no-cache-dir

### q3: 摄像头无法打开？
a: 确保摄像头未被占用，且使用opencv兼容模式，代码中修改为：cap = cv2.videocapture(0, cv2.cap_dshow)（windows专用）

### q4: 打包后下拉框不显示？
a: 打包时添加--hidden-import pyqt6，确保pyqt6完整打包

## 📄 许可证
本项目采用 mit 许可证 - 详见 license 文件

## 🙏 致谢
- mediapipe - 谷歌开源的手势识别框架
- pyqt6 - qt python 绑定库
- opencv - 计算机视觉库
- pyautogui - 键鼠自动化库

---
⭐ 如果觉得项目有用，欢迎点星支持！有问题请提 issues
